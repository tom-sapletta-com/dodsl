# dodsl-contracts

Pure, side-effect-free doDSL schemas, validators, semantic hashes and DSL renderers.
Canonical semantic hashing comes from the pinned, execution-free
`onlydsl-contracts` package. This package must not import runtime, planning,
adapter or application packages.
